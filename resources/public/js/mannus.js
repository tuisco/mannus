$(function(){
  // we will request the ajax for the reviewable -> with trkref/sku
    // put these into the container -> sidebar (mainbar title)
  // we will request the ajax for the reviews -> with trkref/sku
    // put these into the container -> mainbar -> content

});